{
  "name": "hono-storage",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint --fix --ext .ts,.tsx src",
    "lint:check": "eslint --ext .ts,.tsx src",
    "format": "prettier --write \"src/**/*.{ts,tsx}\"",
    "format:check": "prettier --check \"src/**/*.{ts,tsx}\"",
    "build": "tsup ./src/index.ts --format esm,cjs --dts",
    "test": "vitest"
  },
  "keywords": [
    "hono"
  ],
  "author": "sor4chi",
  "license": "MIT",
  "dependencies": {
    "hono": "^3.7.2"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^3.1.3",
    "jsdom": "^22.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "hono@3.7.2": "../../patches/hono@3.7.2.patch"
    }
  }
}
