# Hono Storage for AWS S3 in Cloudflare Workers

```bash
touch .dev.vars
```

Fill in the following environment variables:

```env
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
```

```bash
pnpm i
pnpm dev
```
